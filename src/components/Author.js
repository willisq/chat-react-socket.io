import '../css/author.css';

const Author = ({name}) => {
  return(
        <div className = "authorContainer">
          <h5 className = "author" >{name}</h5>
    	   </div>
)}
export default Author;